version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install
      - npm install -g typescript 

  build:
    commands:
      - echo Building application...
      - npm run build

artifacts:
  files:
    - '**/*'
